name: Sync Fork

on:
  schedule:
    - cron: '0 6 * * 1,3' # “At 06:00 on Monday and Wednesday.”
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.9
        with:
          ignore_fail: false
          auto_approve: false
          base: main
          head: main
          retry_after: 10